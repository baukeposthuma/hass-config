action: fire-dom-event
browser_mod:
  command: popup
  title: Tablet
  style:
    hui-vertical-stack-card:
      $: |
        hui-horizontal-stack-card {
          padding: 0em 2em 2.3em 2em;
        }
      $hui-horizontal-stack-card$: |
        #root {
          justify-content: space-evenly;
        }
  card:
    type: vertical-stack
    cards:
      - type: entities
        state_color: true
        card_mod:
          class: content
        entities:

          - entity: switch.galaxy_tab_a8_screensaver
            secondary_info: last-changed

          - entity: light.galaxy_tab_a8_screen

          - type: custom:slider-entity-row
            entity: light.galaxy_tab_a8_screen
            name: Helderheid
            icon: mdi:brightness-6
            hide_state: false

          - entity: binary_sensor.galaxy_tab_a8_plugged_in

          - entity: sensor.template_fullykiosk_last_app_start

          - type: custom:bar-card
            width: 55%
            height: 2em
            decimal: 0
            unit_of_measurement: '%'
            positions: &bar_card_positions
              icon: outside
              indicator: 'off'
              name: outside
            severity:
              - color: '#6d2525'
                from: 0
                to: 89
              - color: '#303435'
                from: 90
                to: 999
            entity_row: true
            entities:

              - entity: sensor.galaxy_tab_a8_battery_level
                icon: mdi:battery
                tap_action:
                  action: call-service
                  service: homeassistant.update_entity
                  service_data:
                    entity_id: sensor.galaxy_tab_a8_battery_level

          - type: custom:bar-card
            width: 55%
            height: 2em
            decimal: 0
            unit_of_measurement: '%'
            positions: *bar_card_positions
            severity:
              - color: '#6d2525'
                from: 90
                to: 999
            entity_row: true
            entities:

              - entity: sensor.template_fullykiosk_memory
                name: Geheugen - 2GB
                icon: mdi:memory
                tap_action:
                  action: call-service
                  service: homeassistant.update_entity
                  service_data:
                    entity_id: sensor.template_fullykiosk_memory

              - entity: sensor.template_fullykiosk_storage
                name: Opslag - 64GB
                icon: mdi:sd
                tap_action:
                  action: call-service
                  service: homeassistant.update_entity
                  service_data:
                    entity_id: sensor.template_fullykiosk_storage

      - type: horizontal-stack
        cards:
          - type: custom:button-card
            name: Homepagina
            icon: mdi:reload
            tap_action:
              services: |
                [[[
                  return `[[[
                    hass.callService('browser_mod', 'toast', {
                      message: 'Homepage opnieuw laden...'
                    });
                    hass.callService('button', 'press', {
                      entity_id: 'button.galaxy_tab_a8_load_start_url'
                    });
                  ]]]`
                ]]]
            template: icon_name

          - type: custom:button-card
            name: App opnieuw laden
            icon: mdi:reload
            tap_action:
              services: |
                [[[
                  return `[[[
                    hass.callService('browser_mod', 'toast', {
                      message: 'Start Fully Kiosk Browser...'
                    });
                    hass.callService('button', 'press', {
                      entity_id: 'button.galaxy_tab_a8_restart_browser'
                    });
                  ]]]`
                ]]]
            template: icon_name
